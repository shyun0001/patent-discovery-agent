# 변경 이력

## 2026-09-23

### 발명신고서 작성 프롬프트 교체 (`6dd50be`)

특허팀 실무 양식 13개 섹션으로 전면 교체했다. 기존에는 LLM이 JSON 조각을 만들고
템플릿이 조립했으나, 이제 LLM이 문서 본문 전체를 작성한다.

- 기술적 실체 중심 / 입력 근거 준수 / 필수·선택 구분 / 발명 단일성 원칙을 프롬프트에 반영
- 연구자료의 변경 구간마다 근거 식별자 `[E1]~[En]`을 부여해, 6항 "입력 근거" 열과
  청구항 근거가 실제 문서 구간을 가리키도록 했다 (근거 위치 위조 방지)
- 머리말(문서번호·IPC·위험도)과 부록 A(실제 커밋 SHA·원문 링크)는 시스템이 생성
- `OPENAI_MODEL_REPORT` 분리 — 최종 산출물인 신고서 단계만 상위 모델 사용
- LLM 작성 실패 시 기존 템플릿 신고서로 폴백

## 2026-09-22

### 발명신고서 확장 (`c5c628e`)

- 구성요소 표, 동작 순서, 실시예, 청구항 초안(독립항 1 + 종속항 3) 추가
- 특허성 자가평가 표, 산업상 활용 분야, 출원 전 검토 필요 사항 추가
- 분석 근거(소스 경로·커밋 SHA·KIPRIS 원문 링크) 기록으로 추적성 확보

### 사용성·안정성 수정

- 커밋이 1개뿐인 파일 선택 시 비교 불가 사유를 안내 (`d98a925`)
- 신고서 선행기술 표 깨짐 수정 — KIPRIS가 복수 출원인을 `|`로 구분해 반환하여
  마크다운 표 셀이 분리되던 문제 (`699f84c`)
- 배경기술이 기술적 과제와 같은 문장으로 생성되던 중복 제거 (`699f84c`)
- 저장소 파일이 수백 개인 경우를 위한 파일 경로 검색 필터 추가

### Google Drive 웹 앱 OAuth 지원 (`8894479`)

- Desktop app(`installed`)뿐 아니라 웹 애플리케이션(`web`) 클라이언트 시크릿도 지원
- 웹 클라이언트는 등록된 redirect URI와 정확히 일치해야 하므로, 클라이언트 설정에서
  포트와 끝 슬래시를 읽어 로컬 서버에 적용
- API 키 모드 추가 (링크 공개 파일 한정) 및 제약 안내

### KIPRIS 연동 확정 (`3408bf1`)

- 서비스 경로 오타 수정: `patUtiliModInfoSearchSevice` → `patUtiModInfoSearchSevice`
  (오타 시 `resultCode=31 DEADLINE_HAS_EXPIRED`가 반환되어 키 만료로 오인됨)
- 엔드포인트를 `https://plus.kipris.or.kr/kipo-api/kipi` 로, 상세 링크를 kpat 서지 프레임으로 교체
- 페이징 파라미터를 실측 규격(`numOfRows`/`pageNo`)으로 확정
- `resultCode` 매핑(30/31/32)과 `E6007`(키 기간 만료) 신설, 키 오류 시 샘플 데이터로 자동 우회
- 문서 구조 변화(섹션 추가 등)를 발명으로 오인하지 않도록 분석 프롬프트 보강
- LLM 토큰·KIPRIS 호출 사용량 추적과 사이드바 표시

## 2026-09-21

### MVP 구현 (`afc8688`)

- 소스 연동: GitHub REST API v3, Google Drive API v3 (수동 업로드 없음)
- 문서 파싱: `.md` / `.txt` / `.pdf` / `.pptx` / `.docx`, 로컬 `difflib` 기반 Diff
- LLM 4단계: 기술 변화 분석 → 발명 구조화 → 검색어 생성 → 선행기술 유사도 평가
- KIPRIS Plus OpenAPI 선행기술 검색 (국내 특허/실용신안, 오프라인 모드 지원)
- SQLite 캐싱: LLM 응답·KIPRIS 응답·수집 파일 재사용으로 재분석 비용 0
- Streamlit 6개 페이지 + 테스트 74건
